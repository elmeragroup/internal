/**
 * Root-level inferred unions whose members are anonymous object literals.
 * Each member is structure to describe, exactly like an inferred function
 * return or an inferred container element.
 */
export const union = Math.random() > 0.5 ? { a: 1 } : { b: "two" };
