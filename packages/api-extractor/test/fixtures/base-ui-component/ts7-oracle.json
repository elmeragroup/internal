{
  "fixture": "base-ui-component",
  "compiler": "typescript@7.0.2",
  "sourceOracle": "output.json",
  "comparison": "exact",
  "divergence": {
    "code": "TS7_REACT_EXTERNAL_GRAPH",
    "genus": "external-react-graph",
    "reason": "TypeScript 7 resolves the React 19 external conditional and recursive helper graph differently from the pinned TypeScript 6 upstream oracle; deep external React payloads are therefore not treated as a stable equality contract. The recorded difference set was refreshed when container and index-signature extraction landed, again when generic substitution landed, again when Issue 10 module-surface extraction landed, and a fourth time when the Issue 13 external-type ordering landed. The container refresh moved three leaf families inside that same external graph: mapped types over finite key unions report real properties instead of a '[P: string]: any' signature, recursive arrays and tuples keep their own kind when the recursion is cut, and boolean literals encode as the rendered text 'true'/'false'. The generic refresh resolves leaves that earlier extractions gave up on with a wildcard-any fallback, so those leaves now match or approach upstream instead of diverging through 'any': the render-callback state parameters of both components carry their instantiated alias name and namespace ('BaseUIComponent1.State') exactly as the preserved upstream oracle shows - twelve leaves that previously fell back to unaliased 'any' - and the anonymous construct-only arms of React's JSXElementConstructor union resolve to the same bare objects upstream reports instead of unsupported-type fallbacks, which empties this fixture's recoverable-warning oracle from 49 recorded entries to none. The export ordering now reports locally declared value exports first with every remaining export in source order, which moves export ordering toward upstream's value-first rule while preserving TypeScript 7's enumeration order within rank groups (BaseUIComponent1Value therefore stays at position 9 rather than upstream's position 6). The Issue 10 refresh applies upstream's public export-name rule to namespace member exports: 'BaseUIComponent1.Value' now reports its authored public name under its namespace path ({name: 'Value', namespaces: ['BaseUIComponent1']}) exactly as the preserved upstream oracle shows, replacing the earlier leaf that carried the underlying alias name 'BaseUIComponent1Value'. The Issue 13 refresh resolves dependency-owned types as opaque external references BEFORE structural expansion (upstream's own resolver order), so the deep React payload this record exists for now summarizes as named references - React.RefCallback, React.ReactNode, React.HTMLAttributes and their instantiated forms - exactly where the preserved oracle already reported references; the expanded bivarianceHack signature arms and resolved HTMLProps/ReactElement unions earlier records described are gone, and the difference count against the preserved upstream oracle drops from 9126 to 3940. Every remaining shifted leaf is still inside the external React graph this record excludes from the equality contract, and none touches an authored project type.",
    "upstreamPreserved": true,
    "differenceCount": 3940,
    "differenceDigest": "83210bbfac73fd46fcf8ced359e91173614c420f430d42fd6ce9662174b54853"
  }
}
