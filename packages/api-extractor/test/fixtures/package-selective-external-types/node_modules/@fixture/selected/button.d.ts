import type { ForeignDetail } from "fixture-unselected";
import type * as React from "react";

export interface PrimitiveProps {
  /**
   * Allows the primitive to remain focusable while disabled.
   *
   * @default false
   */
  focusableWhenDisabled?: boolean;
  foreignDetail: ForeignDetail;
  onAction?: React.MouseEventHandler<HTMLButtonElement>;
}

export type SelectedMapped = {
  readonly [Key in "mappedOne" | "mappedTwo"]?: string;
};

export interface SelectedKeySource {
  alpha: string;
  beta: number;
}

export interface MixedOwner {
  selectedMember: string;
}

export interface SelectedOwner {
  selectedMember: string;
}
