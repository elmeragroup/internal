import "@fixture/selected/button";

export interface ForeignDetail {
  secret: string;
}

export interface ForeignKeySource {
  foreignAlpha: string;
  foreignBeta: number;
}

export type ForeignMapped = {
  [Key in "foreignMapped"]?: boolean;
};

declare module "@fixture/selected/button" {
  interface MixedOwner {
    unselectedMember: number;
  }
}
