import type * as React from "react";

/** The props a render callback receives; declared in its own file. */
export interface RenderCallbackProps {
  className?: string | undefined;
}

export type RenderFn = (props: RenderCallbackProps) => React.ReactElement;
