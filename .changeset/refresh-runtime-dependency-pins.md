---
"@elmeragroup/internal": patch
---

Refresh the pinned runtime dependencies: `effect` to `4.0.0-rc.115` and `@oxlint/plugins` to `1.82.0`. Both remain external in the bundled entries, so consumers of the next stable receive the new pins.
