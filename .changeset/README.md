# Changesets

Run `pnpm changeset` for changes to published packages. For documentation, CI, or test-only pull requests, use the `no-changeset` GitHub label.

Merging to `main` opens a Version Packages pull request. Canary releases use the separate Publish Canary workflow described in the root README.
